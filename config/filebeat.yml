# Keep the registry on this container's persistent /var/log volume.
path.data: /var/log/filebeat-data
filebeat.shutdown_timeout: 5s

filebeat.inputs:
  - type: filestream
    id: davrods-log
    enabled: true
    paths:
      - /var/log/apache2/apache.access.log
      - /var/log/apache2/apache.error.log
      - /var/log/apache2/apache.audit.log
    parsers:
      - ndjson:
          keys_under_root: true
          add_error_key: true
          overwrite_keys: true
    tags: ["DAVRODS"]

# Disable filebeat monitoring  
logging.metrics.enabled: false

output.logstash:
  hosts: ["elk.dh.local:5044"]
